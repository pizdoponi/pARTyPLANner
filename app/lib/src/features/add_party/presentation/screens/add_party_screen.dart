import 'package:flutter/material.dart';

class AddPartyScreen extends StatelessWidget {
  const AddPartyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("AddPartyScreen"),
      ),
    );
  }
}
