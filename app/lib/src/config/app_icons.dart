import 'package:flutter/material.dart';

class AppIcons {
  static const double bottomNavbarIconSize = 30;
  static const Color iconColor = Colors.white;

  static const Icon archive = Icon(
    Icons.archive,
    color: iconColor,
    size: bottomNavbarIconSize,
  );
  static const Icon search = Icon(
    Icons.search,
    color: iconColor,
    size: bottomNavbarIconSize,
  );
  static const Icon party = Icon(
    Icons.blur_circular_outlined,
    color: iconColor,
    size: bottomNavbarIconSize,
  );
  static const Icon friends = Icon(
    Icons.groups,
    color: iconColor,
    size: bottomNavbarIconSize,
  );
  static const Icon profile = Icon(
    Icons.person,
    color: iconColor,
    size: bottomNavbarIconSize,
  );
  static const Icon add = Icon(
    Icons.add,
    color: iconColor,
    size: bottomNavbarIconSize,
  );
}
